# Esca Syntax Pointer

Syntax notes previously lived here, but this repo now uses a single canonical learning source:

- `docs/language-guide.md`

To build a styled HTML site from that source:

- `make docs-site`

Output:

- `docs/site/index.html`
